// Stub ToolbarComponent: file kept as minimal noop to avoid build errors
// This project currently doesn't import this component. Remove file if you
// prefer permanent deletion.

export default function ToolbarComponent(): null {
  return null;
}